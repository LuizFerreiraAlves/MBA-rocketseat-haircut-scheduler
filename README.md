Runs this project locally.

On the root of your directory run:

`npm run build`<br>
`npm run server`<br>
`npm run dev`<br>

The first command bundles the HTML, CSS, JS and asset contents via webpack.<br>
The second command starts an instance of json-server to mock API calls to your database.<br>
The third command starts the front-end locally.
